# Используйте официальный образ Python
FROM python:3.8-slim

# Установите рабочую директорию в контейнере
WORKDIR /usr/src/app

# Копируйте файлы в контейнер
COPY app.py ./

# Запустите вашу программу при запуске контейнера
CMD [ "python", "./app.py" ]
